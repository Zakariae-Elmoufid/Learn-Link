package org.example.learnlink.modules.admin.entity;

/**
 * Enum representing the target types for moderation actions
 */
public enum ModerationTargetType {
    POST,
    COMMENT,
    QUESTION,
    ANSWER,
    USER,
    MODERATOR
}
